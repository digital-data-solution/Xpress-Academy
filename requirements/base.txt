# Core — needed in every environment.
Django==5.1.15
django-environ==0.14.0
psycopg2-binary==2.9.12
whitenoise==6.12.0
Pillow==12.3.0
django-ckeditor-5==0.2.20
django-admin-sortable2==2.3.1

reportlab==5.0.1
requests==2.34.2
celery==5.6.3
redis==8.1.0

# Added when the phase that needs them starts — listed here so the
# eventual shape of the stack is visible, but NOT installed yet:
#   Phase 8     cloudinary                (image/file storage)
#   —           django-storages           (only if Cloudinary needs the storage backend)
#
# Resend is called directly via requests (already installed) in
# apps/engagement/gateway.py, matching apps/payments/gateway.py's
# pattern — no separate Resend SDK needed.
