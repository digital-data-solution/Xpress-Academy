# Core — needed in every environment.
Django==5.1.15
django-environ==0.14.0
psycopg2-binary==2.9.12
whitenoise==6.12.0
Pillow==12.3.0
django-ckeditor-5==0.2.20
django-admin-sortable2==2.3.1

# Added when the phase that needs them starts — listed here so the
# eventual shape of the stack is visible, but NOT installed yet:
#
#   Phase 4/5   reportlab or weasyprint   (certificate PDF generation)
#   Phase 6     requests                  (Paystack REST calls)
#   Phase 7     celery, redis             (async jobs, Phase 5 tasks)
#   Phase 7     resend                    (email — via the send-gate service)
#   Phase 7     cloudinary                (image/file storage)
#   —           django-storages           (only if Cloudinary needs the storage backend)
